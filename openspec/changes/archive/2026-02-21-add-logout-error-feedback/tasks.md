# Tasks: Add logout error feedback

## 1. App.jsx – handleLogout

- [x] 1.1 In the catch block of `handleLogout`, add `alert("No s'ha pogut tancar la sessió. Torna-ho a intentar.")` (or similar) after `console.error`, so the user sees feedback when logout fails.

## 2. Verify

- [x] 2.1 Confirm the catch block still logs the error and that the new alert is shown on failure (manual check or existing flow).
